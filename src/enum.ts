export enum GameKey {
    UP = 38,
    DOWN = 40,
    LEFT = 37,
	RIGHT = 39,
	SPACEBAR = 32
}

export enum Direction {
    UP,
    RIGHT,
    DOWN,
    LEFT
}

export enum CellType {
    SNAKE,
    APPLE
}

export enum Speed {
    FAST,
    NORMAL,
    SLOW,
    PAUSED
}

